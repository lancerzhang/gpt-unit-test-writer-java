You are working on %s project, you will write one or more unit tests to improve coverage for a class %s and a method with the following details:

%s

You will output the code only, in one code block.
Do not comment on what the code does. The code should be fully functional, don't forget to return all imports, can be executed without any modification.
There is test file already, to avoid conflict, do not use any Lifecycle Method Annotations like 'BeforeEach', add 6 random char at end of each method name.

You have run jacoco, the report is showing that, the following lines are not covered by unit tests:
%s

