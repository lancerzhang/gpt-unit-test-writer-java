You are working on %s project, you will write unit tests to for a new class %s and a method with the following details:

%s

The tests should be as simple as possible, but still cover all the functionality.
You will output the code only, only return one code block, you should include all imports in the code.
Do not comment on what the code does. The code should be fully functional, it can be executed without any modification.
Review the code again, only return one code block, don't miss any imports.